vendor/
node_modules/
dist/
build/
*.cache
.phpunit.result.cache
composer.lock
package-lock.json
yarn.lock
pnpm-lock.yaml
.DS_Store
.idea/
.vscode/
*.log
.env
.env.backup
.phpunit.cache

# Vite temporary files
.vite/

# Inertia Content compiled files (generated, but kept for Vite bundling)
# Don't commit these, but don't delete them during build
# resources/js/.content-compiled/

# Keep node_modules in resources/js for package dependencies
!resources/js/node_modules/
